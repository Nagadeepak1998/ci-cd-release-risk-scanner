apiVersion: v1
kind: Service
metadata:
  name: release-risk-scanner
  labels:
    app: release-risk-scanner
spec:
  selector:
    app: release-risk-scanner
  ports:
    - name: http
      port: 80
      targetPort: 8080

