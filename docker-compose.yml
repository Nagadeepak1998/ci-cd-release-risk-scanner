services:
  release-risk-scanner:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile
    image: ci-cd-release-risk-scanner:local
    ports:
      - "8080:8080"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp

